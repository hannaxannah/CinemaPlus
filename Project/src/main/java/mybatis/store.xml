<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Store.model.store">
	<!-- 전체상품목록 불러오기 -->
	<select id="GetProductList" resultType="Store">
		select * from Product 
	</select>
	
	<!-- 상품 상세정보 불러오기 -->
  	<select id="GetProducDetailByNum" resultType="Store">
		select * from Product where product_code = #{p_code}
	</select>
	
	<!-- 쿠폰 생성 -->
	<insert id="CreateCoupon">
		insert into coupon values
		(Coupon_seq.nextval,#{coupon_name},#{coupon_rate},#{coupon_qty},#{coupon_qty})
	</insert>
	
</mapper>


